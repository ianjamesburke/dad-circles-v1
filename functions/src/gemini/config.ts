/**
 * Gemini Module Configuration
 * 
 * Gemini-specific constants (imported from main config).
 * This file exists for module-level organization but delegates to main config.
 */

import { CONFIG } from '../config';

export const GEMINI_CONFIG = CONFIG.gemini;
